# 贡献指南

欢迎通过 Issue 提交复现步骤、兼容性问题和功能建议。较大改动请先讨论范围；界面与文档目前以中文为主。

## 开发与验证

1. Fork 仓库并从默认分支创建工作分支。
2. 使用 Node.js 22，运行 `npm ci`。
3. 运行 `npm run dev -- --hostname 127.0.0.1`，只使用自己的测试凭据。
4. 修改后执行：

```bash
npm run lint
npm run typecheck
npm test
npm run build
```

涉及 Worker 运行时的改动，还需执行 `npm run preview` 并验证路由读写；不要为了测试运行部署命令。涉及模型调用的验证请说明服务、模型与结果，不要附带密钥。

测试位于 `src/lib/*.test.ts`，使用 Node.js test runner 和 tsx。新增解析、路由或输出行为时补充相应测试；测试不要依赖真实 API 或持久化个人路由数据。新增嵌套测试目录时须同步更新测试命令。

## Pull Request

说明解决的问题、最终行为、验证方式及已知限制。界面改动可附上脱敏截图。同步更新相关文档，保留依赖锁文件；不要提交 `.env*`、`.dev.vars`、`data/` 或构建产物。

提交信息使用中文说明，配合标准前缀，例如 `fix: 修复路由匹配顺序`、`docs: 补充代理调用示例`。参与讨论时尊重他人，聚焦可验证的问题。

提交贡献意味着同意按本仓库 MIT 许可证提供这些贡献。不要提交没有授权的代码、数据或素材。

涉及漏洞与凭据泄露的问题，请遵循 [安全说明](SECURITY.md)，不要在公开 Issue 中贴出敏感细节。

## 依赖维护

`package.json` 将 Next.js 的间接依赖 PostCSS 覆盖为 `8.5.28`，用于避开其原固定版本中的安全公告。升级 Next.js 时重新检查这一覆盖是否仍有必要，并验证样式构建与 `npm audit`。
