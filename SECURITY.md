# 安全说明

Chat2Jev 当前是面向个人或受信任环境的实验工具。维护范围为默认分支；尚无旧版本安全更新承诺。

## 已知边界

- 应用没有内置身份认证、访问权限或限流。任何能够访问接口的用户都可能读取、创建、修改和删除自定义路由；内置路由也可以被覆盖设置。
- 路由在同一部署内共享，可能包含业务问题、映射或提示词，不应存入秘密或个人敏感数据。
- 密钥会随 API 调用经过应用服务端。开启“记住密钥”时，浏览器以明文保存设置；原始请求草稿也会保存，关闭记住密钥并不会自动清除草稿中的凭据。
- 自定义上游地址可导致服务端发起网络请求。当前 URL 校验只阻止部分元数据地址，并不是完整的 SSRF 防护；Node.js 运行时可以访问本地及内网服务。Worker 的网络限制不等同于应用层访问控制。
- 错误信息可能包含上游返回的内容。不要把完整请求头、响应或浏览器存储导出到公开日志和 Issue。
- KV 路由更新缺少跨实例并发控制；文件或内存存储也不提供多租户隔离。

需要共享部署时，请在应用之前配置身份认证（如访问网关）、限流和出站网络限制，使用 HTTPS，并保护所有页面与 API。模型 Key 是上游凭据，不是本应用的访问密码。

## 报告漏洞

请通过仓库 Security 页中的 “Report a vulnerability” 私下报告，前提是维护者已启用 GitHub Private vulnerability reporting。若尚未启用，可先提交不含漏洞细节的 Issue，请维护者提供私下报告渠道。

报告可包含受影响版本、影响和脱敏的复现步骤；不要包含真实密钥、个人信息或可攻击第三方的目标。若发现已泄露的密钥，应先撤销或轮换，再处理历史记录；仅删除当前文件不能消除 Git 历史中的泄露。
